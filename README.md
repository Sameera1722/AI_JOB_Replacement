# AI_JOB_Replacement
Built a multi-class ML model (Random Forest, XGBoost) to predict job automation risk. Performed preprocessing, feature encoding, hyperparameter tuning (GridSearchCV), and 5-fold cross-validation. Evaluated using confusion matrix and multi-class ROC-AUC. Achieved 97.8% CV accuracy with strong generalization.
